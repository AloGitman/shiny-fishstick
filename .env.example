# Copy to .env.local for local development

# Vercel Blob — get this from your Vercel project dashboard after adding Blob storage
BLOB_READ_WRITE_TOKEN=

# JWT signing secret — set to any long random string
JWT_SECRET=change-me-to-a-long-random-string
